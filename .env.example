# Telegram Bot Configuration
# Get your bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=7411752188:AAHpHI_tAo3DX8vx4F_fA1vUVDu9oWLm-u4
TELEGRAM_CHAT_ID=5340401659


# Firebase Configuration
# Path to your Firebase service account key JSON file
FIREBASE_CREDENTIALS=/Users/hariprasad/Downloads/serviceAccountKey..json

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Optional: Proxy Configuration (if needed)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080

# Optional: Request Timeout (in seconds)
# REQUEST_TIMEOUT=30

# Optional: Maximum number of internships to process per run
# MAX_INTERNSHIPS=100

# Optional: Enable/disable specific fetchers
# ENABLE_INTERNSHALA=true
# ENABLE_RSS=true

# Optional: Custom RSS Feed URLs (comma-separated)
# CUSTOM_RSS_FEEDS=feed1_url,feed2_url

# Optional: Keywords to include (comma-separated, overrides default)
# INCLUDE_KEYWORDS=python,java,developer

# Optional: Keywords to exclude (comma-separated)
# EXCLUDE_KEYWORDS=senior,lead,manager

# Optional: Location filters (comma-separated)
# LOCATION_FILTERS=remote,india,united states

# Optional: Minimum and maximum duration in months (0 = no limit)
# MIN_DURATION=1
# MAX_DURATION=6
